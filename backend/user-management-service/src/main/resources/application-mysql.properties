# =========================================
# MYSQL PROFILE - FOR TESTING WITH ARIA_DB
# =========================================
server.port=8080

# =========================================
# MYSQL DATABASE
# =========================================
spring.datasource.url=jdbc:mysql://localhost:3306/user_management_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.username=user_mgmt
spring.datasource.password=StrongPassword123

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true

# =========================================
# FLYWAY MIGRATIONS
# =========================================
spring.flyway.enabled=false

# =========================================
# DISABLE REDIS FOR MYSQL TESTING
# =========================================
spring.data.redis.repositories.enabled=false
spring.session.store-type=none

# =========================================
# JWT CONFIG (Required for JwtUtil)
# =========================================
app.jwt.secret=kV7pL9zR3mX1tU5qW8bC2yJ6fH4nE0sD
app.jwt.expiry-ms=3600000
app.jwt.refresh-expiry-ms=604800000

# =========================================
# MAIL (dummy)
# =========================================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=workwithrvprajwal@gmail.com
spring.mail.password=vclhowpwtmopdqdz
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.default-encoding=UTF-8

# =========================================
# MULTIPART FILE UPLOAD
# =========================================
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
spring.servlet.multipart.enabled=true

# =========================================
# SESSION MANAGEMENT & SECURITY
# =========================================
# Disable URL rewriting to prevent jsessionid in URLs
server.servlet.session.tracking-modes=cookie
# Disable session persistence (since we're using stateless JWT)
server.servlet.session.persistent=false
# Configure session timeout (not used in stateless mode but good practice)
server.servlet.session.timeout=30m
# Disable HTTP session creation for stateless authentication
spring.session.store-type=none

# =========================================
# SECURITY HEADERS
# =========================================
# Add security headers
server.servlet.encoding.enabled=true
server.servlet.encoding.charset=UTF-8
server.servlet.encoding.force=true

# =========================================
# LOGGING
# =========================================
logging.level.org.springframework=INFO
logging.level.com.company=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
logging.level.org.springframework.security=DEBUG

# =========================================
# ENABLE AUTOCONFIGURATION (override any exclusions)
# =========================================
spring.autoconfigure.exclude=
